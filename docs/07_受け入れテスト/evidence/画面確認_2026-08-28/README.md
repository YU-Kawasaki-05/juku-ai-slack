# 画面目視確認 記録（2026-08-28）

実施者: Claude（Playwright MCP で実ブラウザを操作）
環境: ローカル（Next.js `next start` :3200 / Supabase ローカルスタック :54341）
アカウント: `e2e-admin@example.test`（`app_metadata.role = admin`）

> 自動 E2E（86件）と別に、**実際にブラウザを開いて目視で確認**した記録。
> 自動テストは「壊れていないこと」を保証するが、表示の自然さ・文言の妥当性は人（と目視）でしか確認できない。

## 確認できたこと

| # | 証跡 | 確認内容 |
|---|---|---|
| 01 | `01_ダッシュボード_kill_switch稼働中と署名検証エラー記録.png` | AI応答の緊急停止スイッチが「稼働中」バッジ + 停止ボタンで表示される（F-1 / DEC-15）。サイドバーに「ジョブ」（F-4）。最近のエラーに `SLACK_SIGNATURE_INVALID` が並ぶ |
| 02 | `02_生徒編集_ステータス説明が実挙動と一致.png` | ステータスの説明が「無効にすると Bot が応答しなくなり、集計・選択肢からも除外されます」＝実挙動と一致（H-6 修正の結果） |
| 03 | `03_AIプロフィール_氏名を書かない注意書き.png` | **今回追加**: 「ここに入力した内容はそのまま AI に送信されます。生徒の氏名は書かないでください（学年は生徒情報から自動で送られます）」。試験期間の説明が「確認の質問をはさまず、要点をすばやく伝える回答に切り替わります」＝ BR-05-08 / DEC-18 どおり |
| 04 | `04_ジョブ管理_滞留KPIとスイープ実行.png` | 待機中/処理中/失敗の滞留 KPI、スイープ実行ボタン、「この画面を開くと滞留ジョブの回収と古い記録の掃除を自動実行します」の説明（W2-B） |
| 05 | `05_エラー管理_署名検証失敗が記録されている.png` | **今回の P0 修正**: 署名検証失敗が `SLACK_SIGNATURE_INVALID` / 深刻度「エラー」/ 未対応 として記録され、管理画面から観測できる（BR-11-02）。深刻度が仕様どおり `error`（実装は当初 `warning` だった） |
| 06 | `06_利用状況.png` | 質問数・コスト・トークン・画像付き質問の KPI、日別グラフ、エラーコード別の内訳。単価未登録の警告は「未登録モデルの利用が無いため出ていない」＝正しい挙動 |
| 07 | `07_チャンネル紐付け_admin限定.png` | admin でチャンネル紐付け一覧が開ける（EP-07）。既定レポート列あり（H-11） |

## 見つかった軽微な問題

| # | 内容 | 深刻度 | 対応 |
|---|---|---|---|
| 1 | **`/favicon.ico` が 404**。全ページのコンソールにエラーが1件出る | 低（機能影響なし） | 未対応。公開リポジトリ／ポートフォリオとしては用意しておきたい |
| 2 | **`fullPage: true` のスクリーンショットが効かない**。サイドバー付きレイアウトでページ本体ではなく内側の領域がスクロールするため、ビューポート分しか撮れない | 低（証跡取得の手間） | ビューポートを縦長（1200x1900）にして回避。E2E の証跡取得も同じ制約を受けている |
| 3 | `/admin/errors` へ遷移した直後の撮影で、なぜか `/admin/jobs` が写った事象が1度だけ発生 | 低 | 再現せず。MCP 側のタイミング由来と判断し、アプリの不具合とは断定しない |

## 確認しなかったこと

- **staff アカウントでの権限境界**（チャンネル紐付けが拒否される等）→ 自動 E2E の `permissions.spec.ts` / `kill-switch.spec.ts` で担保済み
- **Slack との実連携**（メンション → 回答）→ 実 Slack が必要。`../../90_本番疎通チェックリスト.md` で人が確認する項目
- **画像添付の実挙動** → Vision モデルの実キーが必要。同上
