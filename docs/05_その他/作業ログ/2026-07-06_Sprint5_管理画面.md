# 作業ログ: 2026-07-06（Sprint 5 — 管理画面 基本機能）

## 前段
- Sprint 4（画像対応）を PR #2（develop→main）で main にリリース、CI green
- develop から feature/sprint5-admin を作成

## Sprint 5 実装（FR-14/15、2増分）— 完了条件「UIから生徒登録＋チャンネル紐付け」に集中

### 増分1: バックエンド
- `requireStaff`: Server Action の認証ガード（FR-13, server-only）
- persons: personSchema（空→null 正規化・email 検証）, getPersons/getPerson, create/update Server Action
- channel-bindings: bindingSchema, getBindings（生徒名結合）, create/update Server Action（重複 23505→AC-15-03、更新は name/status のみ=BR-15-01）
- ActionResult 共通型、schema の単体テスト

### 増分2: 管理画面 UI
- PersonForm（作成/編集共用, useActionState）+ 生徒 一覧(SCR-03)/新規/詳細(SCR-04基本)
- BindingForm（生徒選択, 重複エラー表示）+ チャンネル 一覧(SCR-05)/新規(SCR-06)
- サイドバーの生徒管理リンクを /admin/persons に修正
- Server Components でデータ取得 + Server Actions でフォーム送信 + revalidatePath

### 検証
- typecheck / lint / build 通過、`pnpm test` **190 tests pass**
- dev サーバーで未認証 /admin/* → 307 /login、/login 200、ランタイムエラーなしをスモーク確認
- コミット: `f3c6599`（増分1）, `1a087d2`（増分2）

## Sprint 6 送り（意図的除外）
- SCR-04: プロフィール編集③・試験期間設定④・利用推移グラフ⑤（recharts, ai_usage_logs/ai_error_logs 集計）・月次レポート一覧⑥
- ロール（staff/admin）分離、UI の E2E（Playwright, 要認証ユーザー+サーバー）

## レビュー → 修正済み
サブエージェント1体（正確性＋セキュリティ）。認証ガード・Service Role 閉込め・重複検知・BR-15-01 は正しい確認。修正:
- High: AC-15-02（紐付け無効化）の UI 欠落 → `/admin/channels/[id]` 編集ページ + BindingEditForm（name/status のみ）追加、一覧をリンク化
- Low: 生徒一覧から保護者メール列（PII・SCR-03仕様外）を削除、guardianEmail に max(255)
- Medium（記録）: ロール分離未実装・招待制サインアップ前提・015 の RLS コメント不正確 → 保留TODO
- 最終: 190 tests、typecheck/lint/build 通過、commit は fix コミット参照
