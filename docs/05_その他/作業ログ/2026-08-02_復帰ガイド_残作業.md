# 復帰ガイド: リリース準備作業の現状と残作業（2026-08-02 時点）

**このファイルの目的**: セッション中断からの作業再開を最短にする。再開時はまずこのファイルを読むこと。

> **状況更新（最新）**: セッション制限が解け、Wave 4 の3タスクは**再開版エージェントで実行中**。
> - 受け入れテスト（再開版）: WIP の acceptance spec 検証 + tsc エラー4件修正 + ケース一覧設計 + スクショ証拠 + 分割 MD レポート作成中
> - HTML セットアップガイド（再開版）: index.html 検証 + step1〜9 + troubleshooting 作成中
> - docs 整合（再開版）: 03_外部サービス.md 等の WIP 完成 + _index.yml + エラーコード表 + Phase2 提案書作成中
>
> 3体の完了後: オーケストレーターが監査 → `pnpm test` / `pnpm test:e2e` / tsc / lint 全pass 確認 → コミット → **プッシュ** → 最終報告（本ガイド 4.4 参照）。
> もし再び中断した場合: 下記 4.1〜4.3 の指示要点で再々起動すればよい（各エージェントは「WIP を検証してから続きを実装」する設計）。

---

## 1. 現状サマリー

- ブランチ: `feature/release-readiness-setup-docs`（develop から分岐）
- コミット済み: Wave 1（cb9520a）/ Wave 2（21fe345）/ Wave 3（1799ac8）+ このコミット（Wave 4 途中成果物 + 本ガイド）
- テスト: **unit 676 全pass / E2E 56 全pass**（tsc・eslint clean。例外: 書きかけの e2e/acceptance/security-rls.spec.ts に tsc エラー4件 — 下記 4.1）
- ローカル Supabase: ポート 5434x 帯で起動中（api 54341 / db 54342 / studio 54343）。停止していたら `./node_modules/.bin/supabase start` → 必要なら `db reset`（migration 001-032 + seed が通る）
- E2E 実行: `pnpm test:e2e`（build 込み）/ `pnpm test:e2e:nobuild`。env は `.env.test`（無ければ `.env.test.example` が読まれる）

## 2. 完了済み（詳細は各参照先）

| フェーズ | 内容 | 参照 |
|---|---|---|
| 調査 | 実装監査・テスト現状・セットアップ要件逆引き（Opus×3） | 作業ログ `2026-08-02_リリース準備_セットアップドキュメント.md` |
| バグハント | 4レンズ（並行性/外部API/管理画面/データ整合性）で確定バグ約50件 | **バグ台帳** `2026-08-02_バグ台帳.md`（ID 付き正本） |
| Wave 1 | 台帳 A〜H 群の一括修正（5体+縫合パッチ）。migrations 026-030 | コミット cb9520a |
| Wave 2 | kill_switch（DEC-15）/ レート制限 10回/時 / inactive 生徒制御 / jobs 管理画面+スイーパ+掃除。migrations 031-032 | コミット 21fe345 |
| Wave 3 | FR-09 プロフィール・試験期間 UI（個別化の実働化）/ E2E 基盤（ポート衝突解消・テストユーザー自動作成・test:e2e 一発化）+ 認証後 E2E 56件 | コミット 1799ac8 |

重要な仕様裁定（再開時に迷わないこと）:
- **試験期間中は direct モード**（要点・スピード優先）。confirmation ではない（FR-05 BR-05-08 / DEC-18 準拠）
- socratic / confirmation の実働はトピック検出（Phase 2 提案）まで保留。Evaluator は direct では起動しない（A-8、意図的）
- kill_switch は fail-open（読めないときは稼働継続）

## 3. 残作業 = Wave 4（3タスク、いずれも Opus サブエージェントが途中でセッション制限死）

### 4.1 受け入れテスト（docs/07_受け入れテスト/ + e2e/acceptance/）

**目的**: ケース設計 → 必須ケースをスクショ証拠付きで実行 → 分割 Markdown レポート。
**途中成果物（コミット済み、WIP）**:
- `e2e/acceptance/fixtures.ts`, `kill-switch-rate-limit.spec.ts`, `security-rls.spec.ts`（**tsc エラー4件あり: createReport/toast 未定義、toContainText 誤用 — 要修正**）, `slack-flow.spec.ts`, `mock-server.mjs`（Slack/LLM モックサーバー）
- `e2e/fixtures/lock.ts`（新規）、`e2e/kill-switch.spec.ts` と `playwright.config.ts` に変更あり
- `src/shared/lib/env.ts` + `src/shared/lib/slack/client.ts`: `SLACK_API_BASE_URL`（**ループバック限定**の差し替え口。Bot Token 流出防止のガード実装済み・健全）→ Slack フルフロー（署名付き event → ジョブ → モック Slack へ回答 POST）の E2E 再現が可能になっている
- `docs/07_受け入れテスト/evidence/` は空ディレクトリ

**再開時の指示要点**: (1) まずテストケース一覧を設計（AT-XX、必須/推奨、情報源: FR 一覧・バグ台帳・既存 E2E 56件）(2) acceptance spec の tsc エラーを直して全通過させる (3) スクショは `docs/07_受け入れテスト/evidence/AT-XX_*.png`（fullPage、判定ポイントが写ること）(4) レポートは `00_index.md`（判定サマリー）+ カテゴリ別分割 + `90_本番疎通チェックリスト.md`（Slack 実機でしか確認できない項目）+ `91_既知の制約.md` (5) 実 Slack・実 LLM は呼ばない（mock-server.mjs + SLACK_API_BASE_URL を使う）(6) 完了条件: `pnpm test` と `pnpm test:e2e` 全pass

### 4.2 HTML セットアップガイド（docs/06_セットアップガイド/）

**目的**: 顧客先セットアップをステップ分割 HTML で。**このプロジェクトの最重要成果物**。
**途中成果物**: `index.html` のみ作成済み（内容は再開時に検証）。step1〜9 + troubleshooting が未作成。

**再開時の指示要点（構成）**: index / step1 事前準備（LLM プロバイダ選定・**1536次元 Embedding**・公開/非公開チャンネル・Supabase Pro+Vercel Pro 推奨・コスト構造）/ step2 Supabase 作成（**サインアップ無効化が必須手順** — migration 026 ヘッダに理由）/ step3 migration 適用（32本）/ step4 Slack App（**scopes 5種: chat:write, reactions:write, channels:history, groups:history(非公開時), files:read** — channels:read と users:read は不要。manifest YAML 完全掲載。app_mention 購読は不要）/ step5 Vercel + 環境変数完全表（env.ts が正: RESEND は optional 化済み、SLACK_ALERTS_CHANNEL_ID 任意、NEXT_PUBLIC はビルド時埋め込み）/ step6 イベント購読（Request URL = /api/slack/events、Verified にならない原因表）/ step7 管理ユーザー（app_metadata.role SQL）+ 生徒登録 + AI プロフィール + /invite + 紐付け / step8 本番疎通確認 / step9 運用ガイド（kill_switch・レート制限・/admin/jobs・コスト監視・**コスト0円なら MODEL_PRICING 未登録**）/ troubleshooting。
**品質要件**: 自己完結 HTML（CDN 禁止）、日本語、前へ/次へ/目次ナビ、各ステップに所要時間・控える値・完了チェック。**全記述はコード実態を照合**（env.ts / route.ts / client.ts / migrations / constants.ts）。

### 4.3 docs 整合 + Phase 2 提案書

**途中成果物（コミット済み、WIP）**: `docs/03_技術設計/03_外部サービス.md`・`06_運用設計.md`・`08_ローンチ計画.md` に編集途中の変更あり（再開時に diff を確認して続きから）。
**未着手**: `04_セットアップ手順.md`（サインアップ無効化・SLACK_ALERTS_CHANNEL_ID・types:linked の追記）/ `01_要件定義/_index.yml`（FR-14 実態、FR-23〜26 登録）/ FR-14.md 備考 / エラーコード表（`02_外部設計/07_エラー文言設計.md` に AI_PAUSED・RATE_LIMITED・PERSON_INACTIVE・JOB_TIMEOUT 追記。EVALUATOR_FAILED は追記済み）/ **`docs/05_その他/2026-08-02_Phase2提案.md`（新規）**: FR-08 AI月次レポート（8-12日、手動運用で開始可）・FR-05 トピック検出（6日）・Sentry（1日）・小粒改善（ダッシュボード jobs カード、タイムアウト時の🤔除去、SCR-03/04 残り項目、単価未登録警告）

### 4.4 Wave 4 完了後の締め（オーケストレーター作業）

1. 3成果物の監査（受け入れテストの FAIL 有無、ガイドの正確性スポットチェック、docs の事実整合）
2. `pnpm test` + `pnpm test:e2e` + tsc + lint 全pass 確認
3. 作業ログ最終更新 → コミット → **プッシュ**（ユーザー指示: ある程度終わったら都度プッシュ）
4. ユーザーへの最終報告: 全体サマリー / バグ修正数 / テスト数 / 受け入れ判定 / Phase 2 提案 / 本番疎通チェックリストの案内

## 4. 再開手順（コピペ用）

```
1. このファイルと 2026-08-02_バグ台帳.md、2026-08-02_リリース準備_セットアップドキュメント.md を読む
2. git log --oneline -5 で現在地確認（Wave 4 WIP コミットがあるはず）
3. ./node_modules/.bin/supabase status（止まっていれば start）
4. pnpm test で 676 pass を確認
5. Wave 4 の3タスクを Opus サブエージェントで再起動（上記 4.1〜4.3 の指示要点を使用。
   途中成果物があるので「既存の WIP を検証してから続きを実装」と指示すること）
6. 監査 → コミット → プッシュ（4.4）
```

## 5. 運用メモ

- pnpm 11 で `pnpm test` / `pnpm test:e2e` が動く（pnpm-workspace.yaml の allowBuilds 設定済み）
- 型再生成は migration 適用後に `pnpm supabase:types`（ローカル）
- コミット粒度: Wave 単位。プッシュ: ユーザー指示により区切りごと
