# 作業ログ: 2026-06-20

## 作業概要

設計フェーズ最終クリーンアップ。W-01ドリフト解消・エラー文言設計・Sprint計画更新・DB migration作成を完了し、Sprint 0（実装開始）への準備が整った。

---

## 完了した作業

### A. 設計クリーンアップ

#### W-01 解消: ac_test_mapping ドリフト修正
- `docs/02_外部設計/_index.yml` を v2 → v3 に更新
- 登録済み AC: **14件 → 69件**（+55件）
  - FR-03〜12, FR-14〜16 の全 P0 AC を追加
  - FR-23 (BKT/P1) の AC も追加
- 3テーブル（TBL-student_knowledge_states, TBL-student_episodic_memories, TBL-learning_concepts）を tables セクションに追加

#### 「考え中」リアクション仕様追記
- `FR-01_slack-event-receiver.md` に BR-01-06 + AC-01-06 を追加
  - ジョブ登録完了後に 🤔 リアクションを `reactions.add` で追加
- `FR-05_ai-answer.md` に BR-05-08a を追加
  - AI回答送信完了後に 🤔 リアクションを `reactions.remove` で削除

#### エラー文言トーン設計
- `docs/02_外部設計/07_エラー文言設計.md` を新規作成
- 17エラーコード全てに中高生向けの親しみやすい日本語文言を定義
- `USER_FACING_MESSAGES` / `SILENT_ERROR_CODES` の TypeScript実装スケルトンを記載

### B. Sprint計画更新

- `docs/03_技術設計/07_Sprint計画.md` を更新:
  - **Sprint 2** に P(mastery)3モード切替・ワークド例題・🤔リアクションのタスクを追加（FR-26, DEC-23）
  - **Sprint 3** を「レポート・RAG・BKT」に拡張（FR-23タスク群を追加: updateBKT, Evaluator LLM, skip処理）
  - **Sprint 4b「エピソード記憶」** を新規追加（FR-24: セッション終了検出・事実抽出・pgvector保存・ブートシーケンス）

### C. 実装準備（DB migration）

- `supabase/migrations/` ディレクトリを新規作成
- 以下の3ファイルを作成:
  - `017_create_student_knowledge_states.sql` — BKT テーブル（RLS, unique制約, updated_at trigger）
  - `018_create_student_episodic_memories.sql` — エピソード記憶テーブル（HNSW index, superseded_by FK）
  - `019_create_learning_concepts.sql` — FSRSテーブル（Phase 2用、archived_at論理削除）

---

## 設計フェーズ完了状態

| カテゴリ | 状態 |
|---------|------|
| 機能要件 (FR) | FR-01〜26 定義済み |
| 決定事項ログ (DEC) | DEC-01〜25 確定 |
| DB設計 | 15テーブル定義済み、migration 017〜019 SQL作成済み |
| テストマッピング | 69 AC → テストファイル対応付け完了 |
| Sprint計画 | Sprint 0〜6 + Sprint 4b 計画済み |
| エラー文言 | 17コード全て定義済み |

---

## Sprint 0 実装（同日）

### 完了
- `package.json`, `tsconfig.json`, `tailwind.config.ts`, `postcss.config.mjs`, `eslint.config.mjs`, `.prettierrc`, `.gitignore`, `.env.example` を作成し pnpm install 完了
- DB migration 001〜016 を作成（001: persons, 002: student_profiles, 003: reports, 004: pgvector, 005: report_chunks, 006: channel_bindings, 007: thread_sessions, 008: slack_messages, 009: attachments, 010: usage_logs, 011: error_logs, 012: event_receipts, 013: jobs, 014: RLS有効化, 015: RLSポリシー, 016: updated_atトリガー）
- コアファイル: `src/shared/lib/env.ts`, `AppError.ts`, `userMessages.ts`, Supabase 3クライアント（serverClient, browserClient, authServerClient）, `database.types.ts` プレースホルダー
- App Router: `src/app/layout.tsx`, `globals.css`, `page.tsx`（/admin リダイレクト）
- `src/middleware.ts` — Supabase SSR Auth, /admin 保護
- shadcn/ui 手動セットアップ（Button, Input, Label, Card, utils.ts）
- 管理画面: `AdminSidebar`, `AdminHeader`, `AdminHeaderClient`, `/admin/layout.tsx`, `/admin/page.tsx`（ダッシュボード）
- ログイン画面: `/login/page.tsx`, `LoginForm.tsx`
- テスト基盤: `vitest.config.ts`, `playwright.config.ts`, setup.ts, MSW handlers/server, e2e/login.spec.ts
- GitHub Actions CI: `.github/workflows/ci.yml`（lint+typecheck / unit-test / e2e）
- TypeScript 型チェック: **エラー0件**
- Vitest: **9テスト all pass**

### DB接続・動作確認（2026-07-03）
- Supabase Cloud プロジェクト `juku-ai`（ap-northeast-1, PG17）を作成・`.env.local` に鍵設定（project ref は非公開）
- `supabase init` → `supabase link` → `supabase db push` で **migration 001〜019 を全て remote に適用**
  - 途中 017/019 のトリガー関数名が 016 と不整合（`update_updated_at_column` vs `set_updated_at`）で失敗 → 017/019 を `set_updated_at()` に統一して解消
- `supabase gen types typescript --linked` で `database.types.ts` を本物の型（全19テーブル）に再生成
- `@eslint/eslintrc` 未インストールで lint が落ちていた → 追加インストールで解消
- **検証結果（全パス）**:
  - `pnpm typecheck` — エラー0
  - `pnpm test` — 9テスト pass
  - `pnpm lint` — warning/error 0
  - `pnpm build` — 成功（6ページ）
  - dev サーバー: `/login` → 200（フォーム表示）、`/admin` → 307 リダイレクト to /login

### Sprint 0 完了条件 ✅
ローカルで `/login` にログイン画面が表示されることを確認。

### 残作業（外部依存）
- Supabase Auth にスタッフユーザーを1件作成（ログイン実地テスト用）
- Vercel へのデプロイ（環境変数設定含む）
- Slack App 作成・Webhook URL 設定（Sprint 1 で使用）
