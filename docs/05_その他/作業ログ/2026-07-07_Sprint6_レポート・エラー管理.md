# 2026-07-07 Sprint 6: レポート管理・エラー管理・ダッシュボード仕上げ

## 概要

Sprint 6（MVP 完成スプリント）。FR-16 レポート管理（SCR-07/08/09）、FR-17 エラー管理
（SCR-11/12）、SCR-02 ダッシュボード仕上げを実装。

## 設計メモ（実装前の確認結果）

- reports.status は `ai_draft / draft / approved / sent` の4値（migration 003 が正。
  FR-16 本文の draft/published は古い）
- RAG の AI 参照候補は `status IN ('approved','sent') AND is_ai_reference`
  （migration 020/021 の match_report_chunks）→ UI の「承認して保存」= approved が公開相当
- DEC-14: レポート保存時に Embedding を**自動**再生成（確定）。手動再生成ボタン（SCR-09）は
  失敗時・AI生成レポート向けのリカバリ手段として併設
- BR-16-02 / EP-14: 手動 Embedding 再生成は admin のみ → `requireAdmin` を新設
  （~~user_metadata.role~~ → 同日のセキュリティ修正で **app_metadata.role** に変更。
  詳細は `2026-07-07_セキュリティ修正_admin権限判定.md`）
- BR-17-02: severity=info は一覧デフォルトで除外（フィルタで表示可能）
- ai_error_logs にチャンネル名が無い → slack_channel_bindings から名前解決
- Slack スレッド URL は workspace ドメインが必要 → env `SLACK_WORKSPACE_URL`（任意）を新設。
  未設定時はリンク非表示（ID のみ表示）

## 作業内容

- **reports feature**（`src/features/reports/`）: schema（月→月初日正規化、status は
  draft/approved のみ受付）、getReports（生徒/月/状態フィルタ + persons 結合）、
  create/update/rebuildEmbeddings の3アクション（保存時に DEC-14 の自動再生成、失敗時は
  embeddingWarning を返しトースト警告）、ReportForm（Markdown textarea + プレビュー切替、
  「承認して保存」「下書き保存」の2ボタン）、MarkdownContent（react-markdown + remark-gfm、
  raw HTML 無効）、RebuildEmbeddingsButton（確認 Dialog、requireAdmin）、ReportsFilter。
  ページ: 一覧/new/[id]（詳細 + 警告バナー）/[id]/edit
- **errors feature**（`src/features/errors/`）: getErrorLogs（severity 既定 error+warning、
  bindings からチャンネル名解決、countUnresolvedErrors）、resolve/notes の2アクション、
  ResolveErrorButton・ErrorNotesForm・ErrorsFilter、buildSlackThreadUrl（純関数）。
  ページ: 一覧（resolved 行は薄表示）/[id]（raw_error 非表示）
- **ダッシュボード**: SCR-02 の4カード（今日の質問数/今月コスト/未対応エラー/生徒数）+
  最近のエラー5件。getUsageSummary は JST 境界（テスト付き）
- **共通**: StatusBadge にレポート4状態 + severity + 対応状況を追加、Alert に warning variant、
  dialog/textarea を shadcn 追加、requireAdmin 新設、サイドバーのレポート/エラーログを
  本リンク化（利用状況のみ準備中）、env に SLACK_WORKSPACE_URL（任意）
- **依存追加**: react-markdown 10.1.0 / remark-gfm 4.0.1
- **テスト**: 206件全通過（reportSchema 7 / errorLogSchema+slackThreadUrl 5 /
  getUsageSummary JST境界 4 を追加）。typecheck / lint / build も通過
- Playwright + 一時 /preview ルート（削除済み）でライト/ダーク両テーマの実描画を確認
