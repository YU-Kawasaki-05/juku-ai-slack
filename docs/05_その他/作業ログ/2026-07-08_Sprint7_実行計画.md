# 2026-07-08 Sprint 7 実行計画（管理画面仕上げ + FR-20）

## 目的

管理画面 MVP 完成後の「残タスク」を最高水準で片付ける。ユーザー指示に基づき 3 フェーズで実行。

## ブランチ戦略

- `develop`（統合ブランチ）から作業ブランチを切る
  - `feature/sprint7-admin-polish` … Phase 1 + 2（管理画面の品質仕上げ・ドキュメント）
  - `feature/thread-summary` … Phase 3（FR-20 スレッド要約。バックエンド領域）
- 各フェーズ完了時に `develop` へマージ（`--no-ff` で履歴を残す）。push / PR はユーザー判断（本タスクではローカルまで）
- 品質ゲート（typecheck / lint / test / build）を各コミット前に通す

## Phase 1: 私が今すぐ着手できる（feature/sprint7-admin-polish）

| # | タスク | 内容 |
|---|-------|------|
| 1a | バックエンド FR status 更新 | FR-01〜12 の実装/テストを **Workflow で並列検証** し、implemented と確認できたものだけ status 更新。partial は注記 |
| 1b | E2E 拡充 | 認証境界の網羅（全 /admin/* 保護）+ ログイン UX。認証後の通しフローは storageState 方式の雛形を用意（テストユーザー env があれば動く）。方針を docs 化 |
| 1c | 利用状況チャートのテーブルフォールバック | recharts の各チャートに「表で見る」トグル。SR / 印刷 / CVD 向け（dataviz SHOULD） |
| 1d | 会話ログ絞り込み拡張 | 画像あり / 使用モデル / エラーあり での絞り込み（FR-19 主要機能の残り） |

## Phase 2: あなたの設定作業のドキュメント化

- `docs/03_技術設計/` にセットアップ手順書（SETUP-admin-roles など）。3項目:
  1. admin ロール付与（app_metadata.role、SQL + Admin API）
  2. EMBEDDING_* env
  3. SLACK_WORKSPACE_URL env
- コピペで実行できる粒度。確認方法・トラブルシュートも記載

## Phase 3: FR-20 スレッド要約（feature/thread-summary）

前提: Phase 1・2 完了後に着手。

- **調査（Workflow 並列）**: 既存の thread_summary 利用箇所、waitUntil ジョブ基盤（DEC-13）、
  LLM クライアント、episodic memory（DEC-21）との整合、要約生成のトリガー点
- **設計**: いつ・何を入力に・どの粒度で要約するか。コスト・失敗時の扱い（記録失敗は主処理を止めない）
- **実装**: schema/lib/（該当feature）+ テスト。既存パターン厳守
- **レビュー（Workflow adversarial）**: 正確性・セキュリティ（PII/越境）・コストの多観点検証

## 記録

- 各フェーズの結果はこの実行計画に追記、または個別作業ログに残す
- 判断が必要だった点は該当 FR doc / 作業ログに記録
