# 作業ログ: 2026-07-03（Sprint 2 — AI回答生成）

## 作業概要

Sprint 1 完了後、リポジトリ公開体制の整備（個人アカウント化）を経て、Sprint 2「AI回答生成（適応型応答戦略）」の Tutor 側を実装。3増分に分けてコミット。

---

## 前段: リポジトリ・アカウント整備

- `gh` がインターン先アカウントだったため個人アカウント（YU-Kawasaki-05）に切替
- 既存5コミットの著者を個人の GitHub noreply メールに書き換え（未 push だったため安全）
- Public リポジトリ `YU-Kawasaki-05/juku-ai-slack` を作成、main / develop を push
- ブランチ運用: `feature/*` → develop（PR統合）→ main（リリース）
- Sprint 2 は `feature/sprint2-ai-answer` で作業

## 重要な方針変更: LLM プロバイダ未確定

- Anthropic 確定ではない（OpenRouter Fusion / DeepSeek / OpenAI 等検討中）
- → **provider-agnostic な `LlmClient` ポート + OpenAI 互換アダプタ**で実装
- OpenRouter / DeepSeek / OpenAI はすべて OpenAI Chat Completions 互換。`LLM_BASE_URL` / `LLM_API_KEY` / `LLM_MODEL_*` の env 切替でコード変更なしに乗換可能
- 別プロバイダ（Anthropic 直等）はアダプタ追加 + `getLlmClient()` 分岐だけで対応
- 保留事項として `docs/05_その他/保留TODO.md` に記録（本番プロバイダ・モデル・単価・コスト再試算）

---

## 実装内容（増分1〜3）

### 増分1: 土台（純粋・テスト可能）
- `LlmClient` 抽象（types）、OpenAI互換アダプタ、`getLlmClient` ファクトリ（env駆動）
- env をプロバイダ非依存化（`LLM_*` 追加、`ANTHROPIC_API_KEY` 任意化）
- `selectMode`: P(mastery) 3モード判定（direct/socratic/confirmation, DEC-23）
- `calculateCost`: `MODEL_PRICING` によるモデル別コスト（未知モデルは0）

### 増分2: AI回答フロー
- `buildPrompt`: 伴走者フレーミング(DEC-25) + 3モード戦略 + ワークド例題指示(FR-26) + プロフィール/履歴
- `generateAnswer`: buildPrompt → LlmClient.generate
- 読取: `getStudentProfile`(要約+試験前モード), `getMastery`(トピック別P/デフォルト)
- 会話継続: `slack_messages` の `loadThreadHistory` / `saveMessage`
- `logUsage`(ai_usage_logs, FR-12), `stripBotMention`(メンション除去)
- `executeProcessMessage` を暫定返信 → 実AI回答に配線

### 増分3: エラーUX
- `processJob` 最終失敗時、非サイレントなエラーは `getUserFacingMessage` を Slack に返信（内部詳細は出さない, BR-05-12）

### 検証
- `pnpm typecheck` / `pnpm lint` / `pnpm build` 通過、`pnpm test` **100 tests pass**
- コミット: `acd38f2`（増分1）, `d37412b`（増分2）, `430c6f9`（増分3）

---

## Sprint 3 に送った項目（切り分け）

- トピック検出（Evaluator 側）
- Evaluator LLM + BKT 書込（FR-23）
- ミッドコンバセーション・ドロップバック（AC-05-04）
- ワークド例題フェーディング F1〜F4（FR-26, P1）
- RAG チャンク組み込み（FR-10）

→ 現状 P(mastery) はデフォルト 0.2 固定なので実質 direct モードで動作（BKT 未投入のため妥当）。

## レビュー

並列サブエージェント2体（正確性 / セキュリティ）で敵対的レビュー実施 → 指摘反映（本ログはレビュー反映後に確定）。

## 残作業（外部依存）

- LLM プロバイダ確定 + `.env.local` に `LLM_*` 設定 → 実機で AI 回答を確認
- Slack App セットアップ（`保留TODO.md`）
