# 確認事項・作業状況（ユーザー判断が必要なものの唯一の一覧）

最終更新: 2026-08-30（バックアップ保存先を private リポジトリに決定） / ブランチ: `feature/deploy-readiness-audit`（`origin/develop` から分岐）

> **この文書の使い方**
> Claude 側で判断できない項目と、ユーザーが実行する作業手順を1箇所に集約している。
> **まず目次の「🔴 いま必要な作業」を見てください。** 上から順に消化すればデプロイまで進みます。
> ファイルパスはすべて**リポジトリルートからの相対パス**。

<!-- TOC:START -->
## 目次

### 🔴 いま必要な作業（ユーザー）

| | 作業 | 所要 |
|---|---|---|
| 1 | [§3-6. 画像リサイズを入れるかの判断](#s3-6) ← **ご判断のみ**（実装は待機中） | 5分 |
| 2 | [§3-1. バックアップ用 private リポジトリの作成と Secrets 登録](#s3-1) | 20分 |
| 3 | [§2-0. ブランチ保護の設定](#s2-0) | 5分 |
| 4 | [§2-2. Supabase プロジェクトの確認](#s2-2) → [§2-3. migration 適用](#s2-3) | 30分 |
| 5 | [§2-4. Vercel デプロイと環境変数](#s2-4) | 30分 |
| 6 | [§2-5. Slack アプリ作成と #alerts](#s2-5) | 1時間 |
| 7 | [§2-6. `develop` → `main` のマージ](#s2-6) | — |

✅ **[§2-1. OpenAI 実キーでのモデル実測](#s2-1) は完了しました**（2026-08-29）。全体のブロッカーは解除されています。

### 全体

- **[§1. 確定した事項](#s1)** — これまでのご回答と対応状況の一覧
- **[§2. ユーザーが実行する作業](#s2)** — パス → 何を確認 → 操作手順
  - [§2-0 ブランチ保護](#s2-0) ／ [§2-1 モデル実測（✅完了）](#s2-1) ／ [§2-2 Supabase](#s2-2) ／ [§2-3 migration](#s2-3)
  - [§2-4 Vercel](#s2-4) ／ [§2-5 Slack](#s2-5) ／ [§2-6 main へのマージ](#s2-6)
- **[§3. 判断・説明が必要な事項](#s3)**
  - [§3-1 バックアップ保存先（**private リポジトリに決定**・作業手順あり）](#s3-1) ／ [§3-2 画像削減（実装済み）](#s3-2) ／ [§3-3 Vision 未設定の説明](#s3-3)
  - [§3-4 CI の E2E 方針（実装済み）](#s3-4) ／ [§3-5 スタッフの権限範囲](#s3-5)
  - **[§3-6 画像リサイズを入れるか（新規・ご判断が必要）](#s3-6)**
- **[§4. Claude が判断して進めた記録](#s4)**（事後確認でよいもの）
  - [§4-1 DEC-13/16 の訂正](#s4-1) ／ [§4-2 氏名・学年の訂正](#s4-2) ／ [§4-3 その他の判断](#s4-3)
  - [§4-4 EXIF 除去（独立検査 PASS）](#s4-4) ／ [§4-5 CI 実装](#s4-5) ／ [§4-6 バックアップ手順の誤り](#s4-6)
- **[§5. デプロイまでの残り作業](#s5)**

<!-- TOC:END -->

**現在のテスト実測値**（オーケストレーターが自分で実行して確認）
| 項目 | 結果 |
|---|---|
| ユニットテスト | **796 pass** / 76 files |
| E2E（Playwright） | **86 pass** |
| 型チェック / Lint | クリーン |
| migration | 32本すべて適用可（ローカル Supabase で確認） |

---

## <a id="s1"></a>§1. 確定した事項（2026-08-27 のご回答を反映）

| # | 項目 | 決定 | Claude 側の対応 |
|---|---|---|---|
| 1 | **LLM プロバイダ** | **OpenAI 直**（単一プロバイダ。OpenRouter は使わない） | ✅ 文書の矛盾を修正（`docs/05_その他/2026-08-02_本番移行_決定事項と制約.md` §1 の表が「OpenRouter に一本化」のままだった） |
| 2 | **Vercel の参照ブランチ** | **`main`** | ⚠️ デプロイ前に `develop` → `main` の PR が必要（§2-6） |
| 3 | **バックアップ方式** | **GitHub Actions で週次ダンプ**。保存先は**バックアップ専用の private リポジトリ**（R2 は不採用） | ✅ スクリプト・手順書・ワークフロー一式まで完了。残りは §3-1 のユーザー作業のみ |
| 4 | **OpenAI 支出上限** | 設定済み（ユーザー対応済み。hard limit $20 / アラート $5・$12） | ⚠️ hard limit $20 は据え置きでよいが、実測後の想定が $3.88/月 に上がったため **$5 のアラートは平常月でも鳴りうる**。$8 / $14 への引き上げを推奨（決定事項 §4-5） |
| 5 | **月次レポートの起動方式** | **Vercel Cron でよい** | ✅ 「DEC-13 と矛盾する」という前セッションの記述が**誤り**だったので訂正（§4-1） |
| 6 | **氏名の LLM 送信** | **氏名は送らない。学年は送る。未設定なら項目自体を送らない** | ✅ 実装済み（§4-2） |
| 7 | **画像の画質** | **オリジナル維持**。画質を落とさない削減策は検討する | ✅ EXIF 除去を実装（§3-2）。⚠️ **実測でこの決定の再確認が必要になりました** → §3-6 |
| 8 | **Vision 未設定時の挙動** | A案（画像を落としてテキストで回答＋生徒に案内）で問題なし | ✅ 実装済み。ただし §3-3 に補足説明あり |
| 9 | **生徒とチャンネルの紐付け** | admin（ユーザー自身）が実施。スタッフ紐付けは今回不要 | ✅ 画面を admin 限定に実装済み |
| 10 | **Slack ワークスペース** | 招待済み。アラート用チャンネル作成等はユーザーが対応可 | ✅ 手順を §2-5 に記載 |
| 11 | **リポジトリの公開** | **公開のまま**（ポートフォリオとして意図的に公開） | ✅ 実キー混入なしを確認（検出されたのは全てプレースホルダ）。公開前提の設計に切り替え |
| 12 | **GitHub の固定費** | **¥0**（公開リポジトリは Actions 分数・artifact ストレージが無料・無制限） | ✅ 公式ドキュメントで確認。月額合計は **約 3,580 円**（Vercel Pro $20 + OpenAI 約 $3.88）。GitHub による増分はなし（OpenAI 分は §2-1 の実測で $1.84 → $3.88 に引き直し） |
| 13 | **EXIF（メタデータ）除去** | 実施 | ✅ 実装完了・独立検査 PASS（§4-4） |
| 14 | **CI の E2E 方針** | PR は smoke / main は全件 | ✅ 実装完了（§4-5）。ただし §2-0 のユーザー操作が必要 |
| 15 | **GitHub Secrets に認証情報を置く** | 可 | ✅ **バックアップ専用の private リポジトリ側**に置く。公開リポジトリ（本リポジトリ）には置かない（§3-1） |

---

## <a id="s2"></a>§2. ユーザーが実行する作業（パス → 何を確認 → 操作手順）

⚠️ **順序が重要**。特に §2-2（Supabase）と §2-3（migration）は §2-4（Vercel デプロイ）より**先**に完了させる必要がある（理由は §2-4 に実証結果あり）。

### <a id="s2-0"></a>§2-0. 【新規・GitHub の設定画面】ブランチ保護を有効にする

**なぜ必要か**: CI を「PR で E2E smoke を走らせる」構成に変更しましたが、**ブランチ保護が未設定のため、テストが赤でもマージできます**。`gh api` で確認したところ `404 Branch not protected`、ruleset も空でした。テスト戦略ドキュメントの「Block merge」は現状ただの文言です。

**確認すること**: GitHub → Settings → Branches に `main` / `develop` のルールがあるか

**操作手順**
1. GitHub のリポジトリ → **Settings** → **Branches** → **Add branch protection rule**
2. Branch name pattern に `main` を入力
3. **Require status checks to pass before merging** をオン
4. 検索欄で以下3つを追加（**一度 PR を作って CI を走らせないと候補に出てきません**。先に §2-6 の PR を作ってから設定するとスムーズです）
   - `Lint & Type Check`
   - `Unit Tests`
   - `E2E Smoke`
5. 保存後、同じ手順で `develop` にもルールを追加

**補足**: これは workflow ファイルからは設定できない GitHub 側の設定です。所要 5 分程度。

---

### <a id="s2-1"></a>§2-1. ✅ **完了** — OpenAI 実キーでのモデル実測（2026-08-29）

`scripts/check-llm.py` を実キーで実行していただき、**すべて確定しました**。検証コストは **$0.0183** でした。

**確認したかった 7 点の結果**

| # | 確認事項 | 結果 |
|---|---|---|
| 1 | 正式モデル ID | ✅ **`gpt-5.6-luna` / `gpt-5.6-terra` / `text-embedding-3-small` がそのまま実在**。生成も通る。環境変数はこの値でよい |
| 2 | Vision が動くか | ✅ **動作**（画像を送って正しく回答した）。`LLM_MODEL_COMPLEX` に `gpt-5.6-terra` を入れて問題ない |
| 3 | 画像 1 枚の実トークン数 | ✅ **解像度に比例**。64px=4 / 512px=307 / 1,024px=1,228 / **2,048px=4,915**。2,048px までで頭打ちなし |
| 4 | `reasoning_tokens` の消費 | ✅ **両モデルとも 0**。回答が途中で切れる心配はなく、**コード改修も不要**でした |
| 5 | `detail` の実挙動 | ✅ **未指定 / auto / low / high / original の 5 通りすべて受理されるが、トークン数は全て同一**（512px でどれも 307）。つまり **`detail` は実質効きません** |
| 6 | OpenAI 側の内部リサイズ閾値 | ✅ **観測されず**（2,048px まで比例したまま）。**画像 1 枚のトークン数に上限が無い**ということです → §3-6 |
| 7 | WebP が使えるか | ✅ **使えます**。`SUPPORTED_IMAGE_MIMETYPES` から外す必要はありません（以前「除外が必要」とお伝えしたのは誤りでした） |

**この結果を受けた対応**

| 対応 | 状態 |
|---|---|
| `MODEL_PRICING` に luna / terra を登録 | ✅ 完了（`/admin/usage` の「単価が未登録のモデル」警告は出ません。テストで担保） |
| reasoning を切る改修 | ❌ **不要と確定**（`reasoning_tokens = 0`） |
| `max_completion_tokens` の扱い | ✅ **修正不要**（推測どおりそのまま通った） |
| 月額試算の引き直し | ✅ 完了。**$1.84 → $3.88 / 月**（インフラ込み 約 3,280 円 → **約 3,580 円**）。支出上限 $20 は据え置きで足ります |
| 送信前の画像リサイズ | 🟡 **ご判断待ち** → **[§3-6](#s3-6)** |

詳細: `docs/05_その他/2026-08-02_本番移行_決定事項と制約.md` §3-1 / §3-3 / §4

---

### <a id="s2-2"></a>§2-2. Supabase プロジェクトの確認・作成

**確認すること**
1. プロジェクトが既に存在するか（ユーザー側で確認中）
2. **「Allow new users to sign up」が OFF になっているか** ← 最重要
3. リージョン（`ap-northeast-1` 推奨）、プラン（Free）

**なぜ サインアップ OFF が必要か**: `supabase/migrations/026_harden_rls_policies.sql` の前提。ON のままだと誰でもアカウントを作れる（アプリ側の `requireStaff` が role を検証するので管理画面には入れないが、多層防御の1枚目が抜ける）。

**操作手順**: `docs/06_セットアップガイド/step2_supabase作成.html`

**このステップで控える値**: Project URL / anon key / service_role key の3つ

---

### <a id="s2-3"></a>§2-3. DB マイグレーションの適用

**確認すること**: migration 32本が適用され、テーブル・`attachments` バケット・`kill_switches` の初期行ができているか

**操作手順**: `docs/06_セットアップガイド/step3_DBマイグレーション.html`
（`pnpm install` → `supabase login` → `supabase link --project-ref <ref>` → `supabase db push`）

---

### <a id="s2-4"></a>§2-4. Vercel デプロイと環境変数

**ご質問への回答: 「環境変数を設定する」とは何か / デプロイ前か後か**

Vercel の **Project Settings → Environment Variables**（またはインポート画面）で値を登録する作業です。

**デプロイ前に設定が必要です。** 実際に環境変数を空にして `next build` を走らせて検証した結果:

- **必須6個のうち1つでも欠けると、ビルド自体が失敗**します（実行時 500 ではなく、Vercel のビルドが `Collecting page data` で落ちる）。`src/shared/lib/env.ts` がモジュール読み込み時に検証して throw するためです
- さらに **Supabase に実際に疎通できないとビルドが失敗**します（`/admin` の静的化を試みる際に本物のクエリを1回投げるため）。だから §2-2・§2-3 を先に終わらせる必要があります
- 逆に **AI 回答用4個・RAG 用3個・任意3個はビルドをブロックしません**（実行時にしか使われない）。ただし追加・変更したら**再デプロイが必要**です（Vercel は環境変数をデプロイ単位で固定するため）

**必要な環境変数がまとまっているファイル（全19個で一致を確認済み）**

| ファイル | 役割 |
|---|---|
| `src/shared/lib/env.ts` | **実装の正**（Zod スキーマ。必須/任意の最終根拠） |
| `docs/06_セットアップガイド/step5_vercelデプロイ.html` §5-2 | 変数の一覧表＋取得元＋注意点 |
| `docs/05_その他/2026-08-02_本番移行_決定事項と制約.md` §7 | **この案件で入れる実際の値** ← モデル名などはこちらが正 |
| `.env.example` | ローカル開発用のひな形 |

⚠️ **ガイドの例（`gpt-4o-mini` など）ではなく、決定事項ドキュメント §7 の値を使ってください。** ガイドは汎用手順なのでプレースホルダになっています。

**必須6個（ビルド前に必要）**

| 変数 | 取得元 |
|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` | Supabase → Settings → API → Project URL |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | 同上 → anon/public |
| `SUPABASE_SERVICE_ROLE_KEY` | 同上 → service_role（**`NEXT_PUBLIC_` を絶対に付けない**） |
| `SLACK_BOT_TOKEN` | Slack アプリのインストール後に表示（`xoxb-`） |
| `SLACK_SIGNING_SECRET` | Slack アプリ → Basic Information |
| `SLACK_BOT_USER_ID` | `auth.test` の `user_id`（**`U` 始まり**。`B` 始まりの Bot ID を入れると永久に無反応） |

**設定してはいけない / 意味がない変数**
- `ANTHROPIC_API_KEY` — SDK を依存から外したので使われません
- `RESEND_API_KEY` / `RESEND_FROM_EMAIL` — 参照コードがゼロ。**行ごと作らない**（空文字で作るとメール形式検証に失敗し、Slack 連携を含むアプリ全体が 500 になります）
- `SLACK_API_BASE_URL` — 受け入れテストのモック用（ループバック限定）

**操作手順**: `docs/06_セットアップガイド/step5_vercelデプロイ.html`

**ガイドに不足していて、追加で必要な操作**
1. **Production Branch の設定**（Project Settings → Git → Production Branch）を `main` に。ガイドに手順がありません
2. Sensitive 指定の付け方もガイドに手順がありません（`SUPABASE_SERVICE_ROLE_KEY` などは Sensitive 推奨）

**完了条件**: `https://<ドメイン>/login` が表示される

---

### <a id="s2-5"></a>§2-5. Slack アプリの作成とアラート用チャンネル

**操作手順**
- アプリ作成: `docs/06_セットアップガイド/step4_slackアプリ作成.html`
- イベント購読: `docs/06_セットアップガイド/step6_slackイベント購読.html`

**手順の全体像**（読む前に把握しておくと迷いません）

1. api.slack.com/apps で **manifest を貼り付けて作成**（step4 に manifest 全文があります。From scratch の手順も併記）
2. Bot Token Scopes は**5つだけ**: `chat:write` / `reactions:write` / `channels:history` / `groups:history` / `files:read`
   - 非公開チャンネル運用なので `groups:history` が必須。公開用の `channels:history` も入れておくと後で公開チャンネルを追加するときに再インストールが不要
3. ワークスペースにインストール → **`xoxb-` トークンを控える**
4. Basic Information → **Signing Secret を控える**
5. `auth.test` を叩いて **Bot User ID（`U` 始まり）を控える**
6. **ここでいったん止める**（Event Subscriptions は設定しない）
   - 理由: Request URL の登録時に Slack が検証リクエストを送りますが、**署名検証を通すには `SLACK_SIGNING_SECRET` が設定済みのデプロイが必要**です。デプロイ前に登録すると必ず失敗します
7. §2-4 のデプロイ完了後に step6 へ進み、Request URL に `https://<ドメイン>/api/slack/events` を登録して **Verified** を確認
8. Subscribe to bot events に `message.channels` と `message.groups` を追加（**`app_mention` は不要**。メンション検出は本文の文字列一致で行っています）

**アラート用チャンネル（`SLACK_ALERTS_CHANNEL_ID`）**
- 用途: AI の緊急停止スイッチ（kill_switch）を停止/再開したときの通知先
- 値: チャンネル ID（`C` 始まり）。Slack でチャンネルを開き「チャンネル詳細」の最下部に表示される ID、または URL の末尾
- **Bot をそのチャンネルに招待する必要があります**（`/invite @<Bot名>`）。スコープに `chat:write.public` を含めていないため、招待しないと投稿できません
- 未設定でも停止/再開の操作自体は成功します（通知だけ飛びません）

**権限について**: 決定事項では「通常メンバーで足りる見込み」としています。step4 のインストールボタンが `Install to <ワークスペース>` と表示されれば確定、`Request to Install` なら管理者の承認依頼が必要です。

---

### <a id="s2-6"></a>§2-6. `develop` → `main` のマージ（デプロイ前に必須）

**なぜ必要か**: Vercel が `main` を見る決定なので、現状のまま接続すると **23コミット遅れた `main` が本番に上がります**（migration 029〜032、権限強化、セットアップガイド、今回の P0 修正などが全部欠けた状態）。

**操作手順**: このブランチの PR をマージ後、`develop` → `main` の PR を作成してマージ（Claude 側で PR を作成します）。

---

## <a id="s3"></a>§3. まだ判断が必要な事項（追加の質問）

### <a id="s3-1"></a>§3-1. バックアップの保存先 — **バックアップ専用の private GitHub リポジトリに決定**

**先に結論**: **新しく private リポジトリを1つ作り、そこに週次バックアップを置きます。** 費用 ¥0・カード登録不要です。

**なぜ R2 ではないのか**（後から見た人が迷わないように残しておきます）
Cloudflare R2 は当初の第1候補でしたが、**有効化に支払い方法の登録が必要で、お持ちのデビットカードでは通りませんでした**（無料枠内でも登録自体が必須）。技術的には最適でしたが、この環境では使えないため見送りました。
その前に検討していた「公開リポジトリの Actions artifact」も**不採用**です（公開リポジトリの artifact は**認証なしで誰でもダウンロードできる**ため、生徒の個人情報を置けません）。

**なぜ private リポジトリなら大丈夫か**

| | |
|---|---|
| 費用 | GitHub Free は **カード登録不要**。private リポジトリ無制限 / Actions **2,000分/月** / artifact **500MB** が無料 |
| 今回の使用量 | 実行時間 **約20分/月**（無料枠の1%）・容量 **約2MB**（無料枠の0.4%） |
| 閲覧できる人 | artifact のダウンロードには**リポジトリの read 権限が必要**。コラボレータを追加しない限り**オーナーだけ** |
| 二重の防御 | それに加えて、ダンプは **GPG(AES256) で暗号化**してから置きます |

**もう1つ、設計上の重要な決定**: バックアップスクリプトは、公開リポジトリから取ってくるのではなく **private リポジトリに複製**します。
理由は、private 側のワークフローが **DB の全権パスワードを持つ**からです。公開リポジトリのコードを実行する構成にすると、公開側が書き換えられたときに「パスワードを持ったまま他人のコードを実行する」経路になります。複製すればこの経路自体が消えます。
（代わりに **スクリプトを更新したら private 側にもコピーする**運用が要ります。技術設計 §5-3 / §9 に明記しました。）

**保持期間（決定値）**: 週次は artifact に **90日**（約13世代）、**月初の1本だけ Releases** に上げて長期保存（Releases は保持期限なし、24か月で棚卸し）。理由は `docs/03_技術設計/09_バックアップとリストア.md` §6-5。

---

#### ユーザー側の作業（所要 20分）

##### 手順1. private リポジトリを作る（3分）

**パス**: https://github.com/new

**何を確認**: **Private** が選択されていること（ここを間違えると生徒の個人情報が公開されます）

**操作手順**
1. Repository name に `juku-ai-backup` と入力（名前は任意）
2. **Private** を選択
3. 「Add a README file」に**チェックを入れる**（空リポジトリだとファイルを追加しにくいため）
4. **Create repository**
5. ⚠️ このリポジトリに**コラボレータを追加しないでください**。追加した人は生徒の個人情報を全部ダウンロードできます

##### 手順2. `SUPABASE_DB_URL` の値を取ってくる（5分）

**パス**: https://supabase.com/dashboard → 対象プロジェクト → 左サイドバー一番下の **⚙ Project Settings** → **Database**

**何を確認**: **`Session pooler` タブ**を選んでいること。ここが**一番間違えやすい**ところです

**操作手順**
1. 上記の画面で **Connection string** のセクションまでスクロールする
2. タブが `Direct connection` / `Transaction pooler` / `Session pooler` に分かれています。→ **`Session pooler` を選択**
3. 表示された文字列（`postgresql://postgres.<ref>:[YOUR-PASSWORD]@aws-0-<region>.pooler.supabase.com:5432/postgres`）をコピー
4. **`[YOUR-PASSWORD]` の部分を実際の DB パスワードに置き換える**（DB パスワードを忘れた場合は同じ画面の **Database password → Reset database password** で再発行できます。再発行すると既存の接続文字列は使えなくなるので、他で使っていないか確認してから）

| タブ | なぜそれを選ぶ / 選ばない |
|---|---|
| Direct connection | ❌ **IPv6 専用**。GitHub Actions のサーバは IPv4 なので**繋がりません**（Free プランに IPv4 追加オプションはありません） |
| Transaction pooler（`:6543`） | ❌ バックアップに使う `pg_dump` が動きません |
| **Session pooler**（`:5432`） | ✅ **これを使います**。ユーザ名が `postgres.<ref>` の形になっているのが目印 |

##### 手順3. GPG パスフレーズを作る（2分）

**何を確認**: **必ずパスワードマネージャに控える**こと。これを失うとバックアップを復号できず、**バックアップが無いのと同じ**になります

**操作手順**
1. ターミナルで `openssl rand -base64 32` を実行（または任意の32文字以上のランダム文字列）
2. 出てきた文字列をパスワードマネージャに「juku-ai-backup GPG passphrase」等の名前で保存

##### 手順4. Secrets を登録する（5分）

**パス**: 手順1で作った private リポジトリ → **Settings** タブ → 左メニュー **Secrets and variables** → **Actions** → **New repository secret**

**何を確認**: 登録先が **public の marujuku-slack ではなく、手順1の private リポジトリ**であること

**操作手順**（3つ登録します）

| Name | Secret（値） | 必須 |
|---|---|---|
| `SUPABASE_DB_URL` | 手順2でコピーした接続文字列 | ✅ |
| `BACKUP_GPG_PASSPHRASE` | 手順3で作った文字列 | ✅ |
| `SLACK_ALERTS_WEBHOOK_URL` | `#alerts` の Incoming Webhook URL（§2-5 で作るもの） | 任意（失敗時の Slack 通知用。未登録でも GitHub からメールは届きます） |

##### 手順5. ファイルを3つ配置する（5分）

**パス**: 手順1の private リポジトリ

**何を確認**: 配置後、`scripts/backup-db.sh` に**実行権限**が付いていること

**操作手順**（ローカルに clone して push するのが確実です）
```bash
git clone https://github.com/<あなた>/juku-ai-backup.git
cd juku-ai-backup
mkdir -p .github/workflows scripts

# 1) 公開リポジトリからスクリプトを2本コピー（<marujuku-slack のパス> は実際の場所に置き換え）
cp <marujuku-slack のパス>/scripts/backup-db.sh      scripts/
cp <marujuku-slack のパス>/scripts/restore-verify.sql scripts/
chmod +x scripts/backup-db.sh

# 2) ワークフローと README を作る（中身は下記ドキュメントからコピー）
#    .github/workflows/backup.yml  ← 09_バックアップとリストア.md §6-2 をそのまま貼る
#    README.md                     ← 09_バックアップとリストア.md §6-7 をそのまま貼る

git add -A && git commit -m "add weekly db backup" && git push
```

##### 手順6. 手動で1回動かして確認する（5分）

**パス**: private リポジトリ → **Actions** タブ → 左の **Weekly DB backup** → 右上の **Run workflow**

**何を確認**（この3点が揃って初めて「動いた」です）
1. ジョブが**緑**で終わる
2. run の画面の **Summary** に `backup manifest` が出ていて、**`public.persons` などの行数が実際のデータと合っている**（0 行ではない）
3. run の下部 **Artifacts** に `juku-ai-db-backup-...` があり、中身が `*.sql.gpg` 3本 + `*-MANIFEST.txt` 1本

**赤で落ちた場合のよくある原因**
| ログに出るもの | 原因 |
|---|---|
| `connection refused` / タイムアウト | 手順2で `Session pooler` 以外のタブを選んでいる |
| `password authentication failed` | `[YOUR-PASSWORD]` の置き換え忘れ、または記号を含むパスワードのエンコード漏れ |
| `persons が 0 行` | 接続先のプロジェクトが空（別プロジェクトを指している） |
| `BACKUP_GPG_PASSPHRASE が未設定` | 手順4の登録漏れ、または名前のタイポ |

---

**この後の運用**（週1回・10秒）
毎週月曜の朝、Actions のタブを開いて緑になっていること、Summary の `row counts` が前週より不自然に減っていないことを見るだけです。失敗したときは GitHub からメール（＋ `#alerts`）が届きます。

**実行タイミング**: 毎週**月曜 03:35（日本時間）**。深夜で塾の利用がない時間帯かつ、失敗通知を週の頭の朝に受け取れる時刻にしてあります。

詳細（ワークフローの中身・復元手順・落とし穴）: `docs/03_技術設計/09_バックアップとリストア.md`

---

### <a id="s3-2"></a>§3-2. 画像のデータ量削減 — EXIF 除去は**実装済み**／解像度上限は §3-6 へ

EXIF 除去を実装しました。詳細は §4-4。
**解像度の上限設定**は §2-1 の実測が終わったので、**[§3-6](#s3-6) でご判断をお願いします**
（実測の結論: OpenAI 側の内部リサイズは無く、トークン数は解像度に比例して上限が無い）。

---

### <a id="s3-3"></a>§3-3. Vision モデル未設定について（ご質問6への回答）

**何のための設定か**: `LLM_MODEL_COMPLEX` は「**画像が添付された質問のときだけ使うモデル**」です。テキストのみの質問は `LLM_MODEL_DEFAULT` を使います。両者は API キーと base URL を共有するため、**同じプロバイダのモデルでなければなりません**（これが OpenAI 直に統一した理由）。

**どういう場合に未設定になるか**（実質、初回セットアップの設定漏れがほぼ唯一の経路）
1. Vercel で環境変数の行を作り忘れる
2. Preview 環境にだけ設定して Production に入れ忘れる（Vercel は環境ごとに別管理）
3. 変数名のタイポ
4. 値を空文字で登録する

**通常運用で勝手に外れることはあるか**: ありません。一度正しく設定すれば、誰かが Vercel の設定を触るまで維持されます。

**発生頻度の想定**: 初回セットアップ時のみ。運用中に発生する経路はほぼありません。

**ではなぜ対応したのか**: 実装コストが小さく（既に完了）、かつ**未対応時の症状が「画像付き質問の回答自体が失敗する」という分かりにくいもの**だったためです。現在は「画像だけ読まれず、テキストで回答が返り、生徒に理由が伝わる」挙動になっており、万一の設定漏れでも Bot が使えなくなりません。

⚠️ **より重要な別のリスク**: コードは「環境変数が設定されているか」しか見ておらず、**設定されたモデルが本当に Vision 対応かは判定していません**。つまり `LLM_MODEL_COMPLEX` に Vision 非対応のモデル名を入れると、今回の保険は効かず**回答が失敗します**。→ **§2-1 の実測で `gpt-5.6-terra` が実際に画像を読んで回答することを確認済みです**（2026-08-29）。

**ユーザー側で必要な作業**: §2-4 で `LLM_MODEL_COMPLEX` に `gpt-5.6-terra` を設定するだけです（§2-1 で確定）。

---

### <a id="s3-4"></a>§3-4. CI の E2E テスト方針 — **実装済み**（残りは §2-0 の設定のみ）

詳細は §4-5。**ブランチ保護（§2-0）を設定しないと「マージをブロックする」効果は出ません。**

---

### <a id="s3-5"></a>§3-5. スタッフの権限範囲（再確認）

チャンネル紐付けを admin 限定にしました。ユーザーご自身が admin として実施されるとのことなので問題ありませんが、**スタッフだけが出勤している日に新しい生徒のチャンネルを紐付ける必要が生じた場合、運用が止まります**。

現状の想定（スタッフ紐付けは今回不要）で進めますが、運用開始後に必要になったらお知らせください（権限設計の改訂で対応できます）。

---

### <a id="s3-6"></a>§3-6. 【新規・ご判断が必要】画像を送る前にリサイズするか

**実装はしていません。ご判断いただいてから着手します。**

#### なぜ今この論点が出てきたか

§2-1 の実測で 2 つのことが分かりました。

1. **`detail` パラメータは効きません。** 未指定 / `auto` / `low` / `high` / `original` の 5 通りすべてが
   受理されますが、**トークン数は全部同じ**でした（512×512 でどれも 307 トークン）。
   つまり「`detail: "low"` にして安くする」という手は**使えません**。
2. **トークン数は画像の解像度に比例し、上限がありません。**

   | 画像の一辺 | トークン |
   |---:|---:|
   | 512px | 307 |
   | 1,024px | 1,228 |
   | 2,048px | **4,915** |

   2,048px まで比例したままで、OpenAI 側が勝手に縮めてくれる気配はありませんでした。

**結論: コストを左右するレバーは「実際に送る画像の解像度」だけです。**
そして現在のコードは**送信前のリサイズをしていない**ので、**画像 1 枚のトークン数に上限がありません。**

#### 何が問題か

スマホの写真は 3,024×4,032 などです。上の比例式で外挿すると **1 枚あたり約 14,300 トークン**になります
（⚠️ これは外挿値で、実測は 2,048px までです）。画像付きの質問は単価の高い `gpt-5.6-terra`（入力 $2/1M）
を使うので、**1 枚 = 約 $0.029** です。

- **平常時**: 月額は 約 3,280 円 → **約 3,580 円**の見込み（画像率 20%）。**この程度なら問題ありません。**
- **最悪ケース**: レート制限は生徒 1 人あたり **10 問/時**、1 メッセージ **3 枚**まで。
  すべて高解像度写真だと **1 生徒 1 時間で約 43 万トークン ≒ $0.86**。
  20 名が同時に上限まで使うと **$17/時** で、設定済みの hard limit **$20 をほぼ 1 時間で使い切ります**。
  （合計 8MB の上限があるのでスマホ写真では実質 2〜3 枚ですが、桁は変わりません）

つまり**平常運転では問題なく、暴走時に歯止めが無い**という形です。

#### 選択肢

| | 内容 | 画像 1 枚 | 月額（画像率 20%） | 最悪ケース（20 名同時） | 画質 |
|---|---|---:|---:|---:|---|
| **(a)** | 何もしない（現状のまま） | 約 14,300（外挿） | 約 3,580 円 | **$17/時・上限なし** | **オリジナル維持** |
| **(b)** | **長辺 2,048 でキャップ** | 4,915（実測） | 約 3,360 円 | $5.9/時 | ほぼ維持（A4 相当で約 175dpi） |
| **(c)** | 長辺 1,024 でキャップ | 1,228（実測） | 約 3,270 円 | $1.5/時 | **細部が落ちる** |

⚠️ **重要**: 塾の主用途は「**数学の問題の写真**」です。小さい添字・累乗・手書きの文字が読めなくなると、
**コスト削減と引き換えに機能価値そのものが下がります。** ここは単純な金額比較で決めない方がよい論点です。

⚠️ また、**これは §1 の 7 番「画像はオリジナル維持」というご決定の見直しにあたります。**
当時はまだ画像のトークン数が未実測でしたので、新しい材料を踏まえて改めてご判断ください。

#### Claude の推奨: **(b) 長辺 2,048 でキャップ**

理由は 3 つです。

1. **2,048px は実測した中で最大の点**なので、「1 枚 4,915 トークン」という**確定した上限**が手に入ります。
   (a) のままだと上限は外挿でしか語れず、しかも青天井です。
2. **数学の問題写真の判読性はほぼ落ちません。** A4 のノート/問題集を長辺 2,048px で撮った場合、
   解像度は約 175dpi 相当です。手書きの数式や小さい添字を読むには十分な水準で、
   一方 (c) の 1,024px（約 88dpi）は明確に危険域です。
3. **最悪ケースに天井ができます。** hard limit $20 に対して $5.9/時 なら、
   暴走してもアラート（$5 / $12）で気づいて止める時間が確保できます。(a) は 1 時間で上限に達します。

**逆に (a) を選ぶのが妥当なケース**: 「画質は絶対に落とさない」を最優先し、
最悪ケースは hard limit $20 とキルスイッチで受け止める、という運用判断も十分あり得ます。
その場合はリサイズを入れず、`/admin/usage` を運用開始直後に数日見ていただく形をおすすめします。

#### 実装するとどうなるか（ご判断後）

- 入れる場所: `src/features/image-attachments/lib/processAttachments.ts`（**EXIF 除去と同じ経路**）
- 追加依存: **`sharp`**（画像処理ライブラリ。Vercel でも動きます）
- 長辺が閾値以下の画像は**そのまま**（無駄な再エンコードをしない）
- Storage に保存する画像もリサイズ後にするか、保存はオリジナルのままで LLM 送信分だけ縮めるかは、
  合わせて決めさせてください（**推奨は「保存はオリジナル、送信のみ縮小」**。後から見返す証跡が劣化しないため）

---

## <a id="s4"></a>§4. Claude が判断して進めた記録（事後確認でよいもの）

### <a id="s4-1"></a>§4-1. 【訂正】DEC-16 と DEC-13 は矛盾していませんでした

前セッションで「矛盾がある」とお伝えしたのは**私の記述が不正確**でした。DEC-13 の原文は:

> ジョブ実行基盤を Vercel Cron から waitUntil + jobs テーブルに変更 / 理由: **Cron は最短1分間隔の制約があり応答が遅い**

つまり却下されたのは「**Slack の質問に即時応答する経路**に Cron を使うこと」だけで、月次レポートのようなバッチ処理は最初から対象外です。ご指摘のとおり **Vercel Cron で問題ありません**。

複数のドキュメントが「DEC-13 は Cron 不使用」と過度に一般化して書いていたため、「即時応答経路に限る」旨の注記を追加しました。

### <a id="s4-2"></a>§4-2. 【訂正】氏名も学年も、もともと LLM に送られていませんでした

ロードマップの「氏名 + 学年 + 質問内容が送られている」という記述は**誤り**でした。`src/features/student-profiles/lib/getStudentProfile.ts` はスタッフが入力したプロフィール（要約・学習スタイル・得意・苦手・指導メモ）と試験科目だけを送っており、氏名も学年も参照していませんでした。

ご方針に沿って以下を実装しました。

| 内容 | 判断理由 |
|---|---|
| **学年を新たに送る**（未設定なら行自体を出さない） | 中1と高3で説明の粒度が変わるため回答品質が上がる |
| **氏名は送らない**（現状維持） | 回答品質に寄与しない |
| **生徒ID（UUID）も送らない** | LLM から見て無意味な文字列でトークンを消費するだけ。識別子を外部に出す利点がない |
| プロフィール入力フォームに注意書きを追加 | 残る実質的リスクは「スタッフが自由記述欄に実名を書く」ことなので |

⚠️ **運用上の注意（新たに増えた経路）**: 生徒編集画面の**「学年」欄の内容がそのまま LLM に送られます**。ここに「中3 山田」のように氏名を混ぜて書くと氏名が送信されます。**学年欄には学年だけを入力**してください。

### <a id="s4-3"></a>§4-3. その他の判断

| # | 内容 | 理由 |
|---|---|---|
| 1 | 作業ブランチを `origin/develop` から切った | 他環境の作業が develop に入っており、そこが唯一の真実だった |
| 2 | 古い feature ブランチ5本の統合作業をしない | `origin/develop` に対する未マージコミットが**全て0件**で統合すべき差分が無かった |
| 3 | `main` にあって `develop` に無い3コミットは無視 | いずれも PR マージコミット自体で、内容は develop 由来 |
| 4 | 署名検証失敗ログの severity を仕様どおり `error` に | 実装が `warning` で仕様書と食い違っていた。設定ミス時は Bot 全停止なので既定フィルタで目に入る必要がある |
| 5 | AC-08-02 のデッド UI は削除せず非表示 + 理由コメント | FR-08 実装時に戻せるように |
| 6 | `LLM_MODEL_COMPLEX` を Zod の必須にはしない | env 検証の失敗は**アプリ全体が 500** になる設計。画像機能ひとつのために全停止させるのは割に合わない。設定漏れは管理画面のエラーログで検知できる |

---

### <a id="s4-4"></a>§4-4. EXIF（メタデータ）除去の実装 — 独立検査 PASS

**画質を一切落とさない方法を選びました。** 画像処理ライブラリ（sharp）を使うと、どの経路を通っても必ずデコード→再エンコードが走り、JPEG では非可逆になって画質が落ちます。そのため **sharp は使わず、バイト列レベルでメタデータ領域だけを削除**する実装にしました（ピクセルデータの領域には一切触れません）。

| 形式 | 削除するもの | 意図的に残すもの |
|---|---|---|
| JPEG | EXIF・GPS・XMP・IPTC・コメント | JFIF、**ICC プロファイル**、Adobe マーカー（落とすと色再現が変わる＝画質に影響するため） |
| PNG | テキストチャンク・EXIF・タイムスタンプ | 画像本体、色空間情報 |
| WebP | EXIF・XMP | 画像本体、色空間情報 |

解析に失敗した場合・未知の形式の場合は**元のデータをそのまま返す**フェイルセーフにしています（画像が壊れて質問に答えられなくなる方が害が大きいため）。

**削減効果の実測**: 実カメラ相当の 30KB の EXIF を含む画像で **-30,004 バイト**、かつ EXIF を入れる前の原本とバイト単位で完全一致。

**独立検査の結果（別エージェントが実装者の報告を疑って再検証）**
- ピクセル不変: progressive JPEG・CMYK・16bit PNG・実アニメーション WebP など実装者が試していない条件でも、デコード後のピクセルのハッシュが一致
- GPS 除去: 自作バイナリで GPS 座標・MakerNote・埋め込みサムネイルを仕込んだ画像で、**1バイトも残らない**ことを確認
- 壊れた入力 7,020 ケース・ビット反転 9,000 ケース: 例外 0 件・画像破損 0 件
- ミューテーションテスト: 実装を4通り壊すと、対応するテストが全て落ちた（テストが空洞でないことの確認）
- 判定: **PASS。発見された問題なし**

**副次的なメリット**: EXIF の GPS 情報が消えるため、生徒の自宅位置などが LLM やストレージに残らなくなります。

---

### <a id="s4-5"></a>§4-5. CI の E2E 方針の実装

| ジョブ | トリガ | 内容 |
|---|---|---|
| Lint & Type Check / Unit Tests | 常時 | 型チェック・Lint・ユニット 792 件 |
| **E2E Smoke**（新規） | PR | 3 spec / 20 テスト（権限境界・Slack フロー・コスト制御） |
| E2E Fullsuite | main への push | 86 件（従来どおり） |

**実装中に見つかった設計上の穴**
1. **`on.pull_request.branches` が `[main]` だけだった** — 実際の運用は feature → develop → main なので、**feature → develop の PR には E2E が一切走らない**状態でした。`develop` を追加して修正
2. **Playwright は存在しない spec パスを黙って無視して成功扱いにする**（`exit 0`）。ファイル名を変えたときに smoke が静かに縮むため、パス存在チェックを追加
3. **ブランチ保護が未設定** → §2-0（ユーザー操作）

**実行時間の見積もり**（CI 実測ではなく積み上げ）: PR の smoke が約7〜9分、main の全件が約9〜11分。公開リポジトリなので費用は ¥0。

**main への反映**: `main` の `ci.yml` は分岐点以降一度も変更されていないことを blob SHA で確認済み。`develop` → `main` のマージでコンフリクトなく置き換わります。

---

### <a id="s4-6"></a>§4-6. バックアップとリストア — 既存手順書に重大な誤りがありました

**発見1: 既存手順のコマンドでは生徒データが1行も取れていませんでした。**
`docs/06_セットアップガイド/step9_運用ガイド.html` が案内していた `supabase db dump -f backup.sql --linked` は、**既定でスキーマ（テーブルの構造）のみを出力し、中身のデータを含みません**。この手順に従っていたら「バックアップを取っているつもりで、実際には空だった」という最悪のケースになっていました。修正済みです。

**発見2: リストアが丸ごと失敗する条件があります。**
Supabase 内部のテーブルで権限エラーが出ると、トランザクションが巻き戻って**データが1行も入りません**。実際に最初のリストア試行がこれで失敗しました。除外設定を既定に入れて解決済みです。

**実施したリストア訓練（ロードマップの完了条件「取れているだけでは不十分。1回リストアを試すこと」を満たすため）**

検証用に別の Supabase を立てて（既存のローカル環境には触らず）、次を実施しました。

1. 代表データを投入（日本語・改行・引用符を含む文章、1536次元のベクトル、自己参照する外部キー、認証ユーザー）
2. バックアップを取得
3. **`db reset` で全損をシミュレート**
4. バックアップから復元
5. 検証SQL で照合 → **17テーブルの行数・本文のハッシュ・ベクトルの次元と内積・RLS ポリシー・インデックス・権限すべて一致**

**作成物**
| ファイル | 内容 |
|---|---|
| `scripts/backup-db.sh` | 認証情報を含まないバックアップスクリプト（環境変数から読む）。失敗時は非ゼロ終了し部分出力を破棄 |
| `scripts/restore-verify.sql` | 復元後の判定クエリ（読み取り専用） |
| `docs/03_技術設計/09_バックアップとリストア.md` | 手順書本体。取得・復元6手順・GitHub Actions の YAML 例・落とし穴 |

保存先も決まりました（**バックアップ専用の private リポジトリ**）。残りは §3-1 のユーザー作業（20分）だけです。

---

## <a id="s5"></a>§5. デプロイまでの残り作業

### Claude 側でこれから行うこと
- [x] Playwright で実際に画面を開いて目視確認（証跡: `docs/07_受け入れテスト/evidence/画面確認_2026-08-28/`）
- [x] 週次バックアップのワークフロー（private リポジトリ用の YAML 一式。`09_バックアップとリストア.md` §6-2 にコピペ可能な形で用意）
- [ ] favicon の追加（全ページで 404 が出ている。ポートフォリオ用の polish）
- [ ] `develop` → `main` の PR 作成（デプロイ前に必須）

### 完了済み（今回）
- [x] EXIF 除去（独立検査 PASS）
- [x] CI の E2E smoke / fullsuite 分離
- [x] バックアップスクリプト + リストア手順 + **実際のリストア訓練**
- [x] 学年のみ LLM 送信（氏名・生徒IDは送らない）
- [x] ドキュメント整合（LLM プロバイダ・Vision 挙動・Cron の誤解・テスト実測値）
- [x] OpenAI 実測値の反映（`MODEL_PRICING` 登録・試算引き直し・`detail`/reasoning/WebP の記述修正）

### ユーザー側の作業（§2 参照）
- [ ] §2-0 ブランチ保護の設定（GitHub の設定画面。所要5分）
- [x] §2-1 OpenAI 実キーでのモデル実測 ← ✅ **完了（2026-08-29）。ブロッカー解除**
- [ ] §2-2 Supabase の確認（作成済みか）
- [ ] §2-3 migration 適用
- [ ] §2-4 Vercel デプロイ + 環境変数
- [ ] §2-5 Slack アプリ + `#alerts` チャンネル
- [ ] §3-1 バックアップ用 private リポジトリの作成 → Secrets 登録 → ファイル配置 → 手動実行で確認（20分）
- [ ] §3-6 画像リサイズを入れるかのご判断（実装は待機中）
- [ ] 疎通確認（`docs/07_受け入れテスト/90_本番疎通チェックリスト.md` の必須12項目）

### 今回スコープ外（Phase 2）
`docs/05_その他/2026-08-02_Phase2提案.md` を参照。FR-08 AI月次レポート自動生成、FR-05 socratic/confirmation モード、外部APM など。
