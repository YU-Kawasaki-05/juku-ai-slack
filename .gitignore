# Dependencies
node_modules/
.pnpm-debug.log*

# Next.js
.next/
out/
next-env.d.ts

# Environment variables (secrets — never commit)
.env
.env.local
.env.*.local
.env.development
.env.production
# E2E 用のローカル上書き（雛形の .env.test.example はコミット対象）
.env.test

# Supabase (local runtime + linked project ref)
.supabase/
supabase/.temp/
supabase/.branches/

# Build artifacts
dist/
build/
*.tsbuildinfo
tsconfig.tsbuildinfo

# Testing
coverage/
test-results/
playwright-report/
playwright/.cache/
e2e/.auth/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS
.DS_Store
Thumbs.db

# IDE / editor
.vscode/
.idea/
*.swp

# Claude Code local settings (personal, machine-specific)
.claude/settings.local.json

# Claude Code project settings (contains local absolute paths)
.claude/settings.json

# Claude Code local context (anonymization mapping — never publish)
CLAUDE.local.md

# DB バックアップ（生徒の個人情報。このリポジトリは PUBLIC なので絶対にコミットしない）
# scripts/backup-db.sh は既定でリポジトリ外に出力し、リポジトリ内への出力を拒否する。
# これは手で置いてしまった場合の保険。
/backups/
/juku-ai-backups/
juku-ai-backup-*.sql
juku-ai-backup-*.sql.gpg
juku-ai-backup-*-MANIFEST.txt
